<resources>

    <style name="Title">
        <item name="android:textSize">@dimen/font_title</item>
        <item name="android:textColor">@color/gray_dark</item>
    </style>

    <style name="Subtitle">
        <item name="android:textSize">@dimen/font_subtitle</item>
        <item name="android:textColor">@color/gray</item>
    </style>

    <style name="RateInput">
        <item name="android:textSize">@dimen/font_rate_input</item>
        <item name="android:textColor">@color/gray_dark</item>
        <item name="android:textColorHint">@color/gray_light</item>
        <item name="android:backgroundTint">@color/gray_light</item>
    </style>
</resources>
